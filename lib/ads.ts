/**
 * 애드센스 광고 단위.
 *
 * 퍼블리셔 ID 는 keywordegg 와 같은 계정(ca-pub-9130836798889522)을 쓴다.
 * 다만 **광고 단위(슬롯)는 이 사이트용으로 새로 만들어야 한다.** 다른 사이트의
 * 슬롯을 그대로 쓰면 애드센스 리포트에서 수익이 섞여 어느 사이트가 버는지
 * 구분되지 않는다.
 *
 * 슬롯 ID 가 빈 문자열이면 <Adsense> 가 아무것도 그리지 않는다.
 * 애드센스에서 단위를 만든 뒤 여기 값을 채우면 그때부터 노출된다.
 */
export const AD_CLIENT = "ca-pub-9130836798889522";

export const AD_SLOTS = {
  /** 홈 상단·하단 */
  home: "9710495711",
  /** 글/지역 본문 맨 위 */
  top: "4514009355",
  /** 소개글·CTA 다음 */
  middle: "8397414046",
  /** 본문 중간(두 번째 h2 아래)·목록 하단 */
  bottom: "4447198779",
} as const;

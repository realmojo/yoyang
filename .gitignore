# dependencies
/node_modules
/.pnp
.pnp.*

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files
.env*
!.env.example

# typescript
*.tsbuildinfo
next-env.d.ts

# cloudflare / opennext
.open-next/
.wrangler/
.dev.vars
cloudflare-env.d.ts

# 공공데이터포털에서 내려받은 원본 CSV (용량이 크고 재취득 가능).
# 디렉터리 자체는 남긴다 — 적재 스크립트가 여기를 읽는다.
/data/raw/*
!/data/raw/.gitkeep
tmp/*

naver-indexing/*